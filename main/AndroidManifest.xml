<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.universityscholarshipmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniversityScholarshipManagementSystem">
        <activity android:name=".controller.SaveRegister"></activity>
        <activity android:name=".controller.datasave" />
        <activity android:name=".controller.MainActivity2" />
        <activity android:name=".controller.Connected" />
        <activity android:name=".controller.Signin2" />
        <activity android:name=".controller.SignIn" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>